# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=INFO

# Data paths (relative to project root)
DATA_DIR=./data
ARTIFACTS_DIR=./symptom_classifier/artifacts

# CORS (comma-separated origins, use * for development only)
CORS_ORIGINS=*
